limereg 1.2.1

This version uses liblimereg. (The version before was not split up between an algorithm library and an executable).

Issues that will be solved soon:
- Currently only square images are supported (limitation of liblimereg)
